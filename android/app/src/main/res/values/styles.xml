<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <!-- Use a fullscreen-scaled drawable for the splash image -->
        <item name="bootSplashLogo">@drawable/bootsplash_fullscreen</item>
        <!-- Ensure the activity window background shows our fullscreen bootsplash drawable immediately -->
        <item name="android:windowBackground">@drawable/bootsplash_fullscreen</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>
