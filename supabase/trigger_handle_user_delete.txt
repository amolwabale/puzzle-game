BEGIN

DELETE FROM public.ticket_chat
  WHERE user_id = OLD.id;

  DELETE FROM public.ticket
  WHERE user_id = OLD.id;

  DELETE FROM public.tenant_room_mapping
  WHERE user_id = OLD.id;

  DELETE FROM public.bill
  WHERE user_id = OLD.id;

  DELETE FROM public.meter_reading
  WHERE user_id = OLD.id;

  DELETE FROM public.tenant
  WHERE user_id = OLD.id;

  DELETE FROM public.room
  WHERE user_id = OLD.id;

  DELETE FROM public.setting
  WHERE user_id = OLD.id;

  DELETE FROM public."User"
  WHERE user_id = OLD.id;

  RETURN OLD;
END;